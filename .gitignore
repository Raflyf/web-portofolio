# Operating System Files
.DS_Store
Thumbs.db

# Node / Package managers
node_modules/
npm-debug.log*

# Environment & Secrets (NEVER COMMIT PRODUCTION SECRETS)
# Covers .env.local via `.env.*` AND `*.local` (both patterns below)
.env
.env.*
!.env.example
*.local
*.log
ngrok_auth.md
*.auth*
.system_generated/
.agents/
skills-lock.json
.mcp.json
.roo/
.claude-flow/

# Local utility scripts (not part of Vercel deployment)
generate_favicons.py
prompt.md

# Scratch / experimental workspace (ephemeral, not for production)
scratch/

# Vite build output (never commit generated artifacts)
dist/
